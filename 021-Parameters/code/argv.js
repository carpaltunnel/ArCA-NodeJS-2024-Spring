console.log(process.argv);


/*
// Easy example of retrieving "named" parameters
const userParam = '--username';
const passParam = '--password';

const username = process.argv[process.argv.indexOf(userParam) + 1];
const password = process.argv[process.argv.indexOf(passParam) + 1];

console.log(`Username : ${username}`);
console.log(`Password : ${password}`);
*/